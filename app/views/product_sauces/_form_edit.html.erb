<%= form_with(model: product_sauce, local: true) do |form| %>
  <% if product_sauce.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_sauce.errors.count, "error") %> prohibited this product_sauce from being saved:</h2>

      <ul>
        <% product_sauce.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
      <h1><%=product_sauce.product.name%></h1>

      <div class="field">
      <% options = options_for_select(@sauces.collect{ |p| [p.name, p.id] }) %>
      <%= form.select :sauce_id,options %>
      </div>

    <div class="field">
      <%= form.label :quantity %>
      <%= form.text_field :quantity %>
    </div>

    <div class="actions">
      <%= form.submit 'Update' %>
    </div>
<% end %>
